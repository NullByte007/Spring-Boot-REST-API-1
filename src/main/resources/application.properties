
server.port=9090


# All the database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/sbapi1
spring.datasource.username=root
spring.datasource.password=aniket8411
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate configurations
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# this property is added because it failed the comment module due to exception of empty bean
# Use this property when using Fetch type as 'LAZY'
# spring.jackson.serialization.FAIL_ON_EMPTY_BEANS=false

